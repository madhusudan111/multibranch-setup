pipeline {
    agent any

    stages {
        stage('Echo Branch Name') {
            steps {
                script {
                    
                    echo "Branch Name: $BRANCH_NAME"
                }
            }
        }
    }
}
